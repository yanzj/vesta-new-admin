version 'unspecified'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {

    compile project(":vesta.domain.pay")

    testCompile group: 'junit', name: 'junit', version: '4.11'
}