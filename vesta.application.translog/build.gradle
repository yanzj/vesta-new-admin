version 'unspecified'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {

    compile project(":vesta.common.utility")
    compile project(":vesta.common.result")

    compile project(":vesta.repository.translog")
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
